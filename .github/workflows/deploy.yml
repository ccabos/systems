name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - run: pip install "mkdocs>=1.5,<2.0" mkdocs-material pymdown-extensions mkdocs-with-pdf

      - name: Install WeasyPrint system dependencies
        run: sudo apt-get install -y libpangocairo-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Mermaid CLI and Chromium dependencies
        # mmdc renders Mermaid diagrams to PNG server-side because
        # WeasyPrint cannot execute the client-side Mermaid script.
        # See projects/systems-introduction-book/hooks.py.
        run: |
          sudo apt-get install -y \
            libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libxcomposite1 \
            libxdamage1 libxrandr2 libgbm1 libxkbcommon0 libpango-1.0-0 \
            libcairo2 libasound2t64
          npm install -g @mermaid-js/mermaid-cli

      - name: Build MkDocs site
        working-directory: projects/systems-introduction-book
        env:
          ENABLE_PDF_EXPORT: 1
        run: mkdocs build --site-dir ../../site

      - name: Commit generated PDF to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p dist
          cp site/Systems_Thinking_Book_MkDocs.pdf dist/
          git add dist/Systems_Thinking_Book_MkDocs.pdf
          git diff --staged --quiet || git commit -m "chore: update generated PDF [skip ci]"
          git push

      - uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
